apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: example-app-2
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: example-app-2
  policy:
    semver:
      range: 1.0.x